const grid = document.getElementById("container");

for (i = 0; i < 9; i++) {
  const div = document.createElement("div");
  div.id = "pixel";
  grid.appendChild(div);
  // const divBoxStyle = document.getElementById('pixel');
}
